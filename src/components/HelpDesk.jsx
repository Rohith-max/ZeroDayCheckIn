import React from "react";
import Avatars from "./Avatars";

// Temporary: reuse first state's layout until further instructions
const HelpDesk = () => {
  return <Avatars />;
};

export default HelpDesk;
